<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    </style>

    <style name="buttons_style">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">@dimen/activity_object_margin</item>
        <item name="android:onClick">buttonClick</item>
        <item name="android:textColor">@color/GRAY_M11</item>
        <item name="android:textSize">@dimen/activity_text_medium</item>
    </style>

    <!--
    -->

</resources>
